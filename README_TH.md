# XO Arena - เกมส์ OX ออนไลน์

เกมส์ Tic-tac-toe (OX) บนเว็บที่รองรับการเข้าสู่ระบบด้วย Google และ Facebook พร้อมระบบคะแนน การจัดอันดับ และบันทึกสถิติการเล่นแบบถาวร

## คุณสมบัติ

- **เข้าสู่ระบบหลายช่องทาง**: รองรับ Google และ Facebook ผ่านมาตรฐาน OAuth 2.0
- **ท้าชิงกับบอท**: เล่น OX กับ AI ที่มีระดับความยากปรับได้
- **ระบบคะแนน**: 
  - ชนะได้ +1 คะแนน, แพ้เสีย -1 คะแนน, เสมอไม่มีการเปลี่ยนแปลง
  - โบนัสชนะติด: ชนะ 3 ครั้งติดกันได้โบนัสเพิ่ม +1 คะแนน แล้วเริ่มนับใหม่
- **บันทึกถาวร**: ข้อมูลผู้เล่นทั้งหมดเก็บใน SQLite (`data/xo.sqlite`)
- **ระบบจัดอันดับ**: จัดอันดับตามอัตราชนะ คะแนน และจำนวนชนะ (Rookie → Bronze → Silver → Gold → Platinum → Diamond → Legend)
- **ชื่อเล่น**: เปลี่ยนชื่อที่แสดงได้จากเมนูตั้งค่า (ค่าเริ่มต้น `playerxxxxx`)
- **ดีไซน์ Responsive**: ธีมสีเข้มสไตล์ Neon ใช้งานได้ทั้งคอมพิวเตอร์และมือถือ
- **กระดานคะแนนสด**: ดู Top 5 พร้อมอันดับของคุณที่หน้าหลัก หรือดูรายละเอียดทุกคนที่หน้าแยก

## เริ่มต้นใช้งาน

### 1. ติดตั้ง Dependencies

ใช้ npm:
```bash
npm install
```

หรือใช้ yarn:
```bash
yarn install
```

### 2. ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์โปรเจกต์ แล้วใส่ค่าดังนี้:

```ini
# Google OAuth
GOOGLE_CLIENT_ID=google-client-id-ของคุณ
GOOGLE_CLIENT_SECRET=google-client-secret-ของคุณ
GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# Facebook OAuth
FACEBOOK_APP_ID=facebook-app-id-ของคุณ
FACEBOOK_APP_SECRET=facebook-app-secret-ของคุณ
FACEBOOK_CALLBACK_URL=http://localhost:3000/auth/facebook/callback

# Session Secret (ใช้ข้อความสุ่มที่ยาวและซับซ้อน)
SESSION_SECRET=ข้อความลับสำหรับ-session

# Port
PORT=3000
```

#### การขอ OAuth Credentials:

**Google OAuth:**
1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. สร้างโปรเจกต์ใหม่และเปิดใช้งาน Google+ API
3. สร้าง OAuth 2.0 Credentials
4. เพิ่ม Authorized Redirect URI: `http://localhost:3000/auth/google/callback`
5. คัดลอก Client ID และ Client Secret

**Facebook OAuth:**
1. ไปที่ [Facebook Developers](https://developers.facebook.com/)
2. สร้างแอปใหม่และเพิ่มฟีเจอร์ "Facebook Login"
3. เพิ่ม Valid OAuth Redirect URI: `http://localhost:3000/auth/facebook/callback`
4. เพิ่ม App Domain: `localhost`
5. คัดลอก App ID และ App Secret จากหน้า Settings → Basic

### 3. รันโปรแกรม

ใช้ npm:
```bash
npm start
```

หรือใช้ yarn:
```bash
yarn start
```

สำหรับโหมดพัฒนาที่มีการ restart อัตโนมัติ:
```bash
npm run dev
# หรือ
yarn dev
```

### 4. เล่นเกมส์

1. เปิดเว็บเบราว์เซอร์ไปที่ `http://localhost:3000`
2. เข้าสู่ระบบด้วย Google หรือ Facebook
3. ท้าชิงบอทและสะสมคะแนน
4. ดูกระดานคะแนนเปรียบเทียบกับผู้เล่นคนอื่น (Top 5 + อันดับคุณ แสดงที่หน้าหลัก)
5. เปลี่ยนชื่อเล่นได้จากเมนู Settings
6. คลิก "View all players" เพื่อดูกระดานคะแนนแบบเต็มพร้อมสถิติละเอียด


## โครงสร้างโปรเจกต์

```
XO/
├── data/
│   └── xo.sqlite          # ฐานข้อมูล SQLite (สถิติผู้เล่น & ชื่อเล่น)
├── public/
│   ├── index.html         # หน้าเล่นเกมส์หลัก
│   ├── scoreboard.html    # หน้ากระดานคะแนนแบบเต็ม
│   ├── app.js             # ตรรกะเกมส์ฝั่ง Frontend
│   └── styles.css         # สไตล์ธีมสีเข้ม
├── server.js              # Express server พร้อม OAuth & API endpoints
├── package.json           # Dependencies
├── .env                   # Environment variables (ไม่ commit ขึ้น git)
├── README.md              # คู่มือภาษาอังกฤษ
└── README_TH.md           # คู่มือภาษาไทย (ไฟล์นี้)
```

## API Endpoints

### Authentication
- `GET /auth/google` - เริ่มกระบวนการ login ด้วย Google
- `GET /auth/facebook` - เริ่มกระบวนการ login ด้วย Facebook
- `GET /auth/google/callback` - Callback จาก Google OAuth
- `GET /auth/facebook/callback` - Callback จาก Facebook OAuth
- `POST /auth/logout` - ออกจากระบบ

### User & Game
- `GET /api/user` - ดึงสถิติผู้เล่นปัจจุบัน (ต้อง login)
- `PUT /api/user/nickname` - เปลี่ยนชื่อเล่น (ต้อง login)
- `POST /api/game/result` - ส่งผลการแข่งขัน (ต้อง login)

### Scoreboard
- `GET /api/scores` - ดึงคะแนนผู้เล่นทั้งหมด
- `GET /api/scores/summary` - ดึง Top 5 + สถิติผู้เล่นปัจจุบัน
- `GET /scoreboard` - แสดงหน้ากระดานคะแนนแบบเต็ม

## คำอธิบายระบบ

### ระบบคะแนน
- **ชนะ**: +1 คะแนน
- **แพ้**: -1 คะแนน
- **เสมอ**: ไม่มีการเปลี่ยนแปลงคะแนน
- **Streak Bonus**: ชนะ 3 ครั้งติดต่อกันได้โบนัส +1 คะแนน (streak จะ reset เป็น 0)

### ระบบจัดอันดับ
ระบบจะคำนวณจากอัตราชนะ, คะแนน, และจำนวนชนะ:

**การคำนวณ Win Rate:**
```
Win Rate = จำนวนชนะ / (จำนวนชนะ + จำนวนแพ้ + จำนวนเสมอ)
Win Rate = Wins / (Wins + Losses + Draws)
```

**ตัวอย่าง:**
- ชนะ 7 ครั้ง, แพ้ 2 ครั้ง, เสมอ 1 ครั้ง → Win Rate = 7/10 = 70%
- ชนะ 8 ครั้ง, แพ้ 2 ครั้ง, เสมอ 0 ครั้ง → Win Rate = 8/10 = 80%

**อันดับตามเกณฑ์:**

| อันดับ | เงื่อนไข |
|--------|----------|
| **Legend** | Win Rate ≥ 80% และ Score ≥ 15 |
| **Diamond** | Win Rate ≥ 70% และ Score ≥ 10 |
| **Platinum** | Win Rate ≥ 60% และ Score ≥ 6 |
| **Gold** | Win Rate ≥ 50% และ Score ≥ 3 |
| **Silver** | Win Rate ≥ 40% และ Score ≥ 0 |
| **Bronze** | ชนะอย่างน้อย 1 ครั้ง |
| **Rookie** | ยังไม่เคยชนะ |

### การแสดงผลกระดานคะแนน
- **หน้าหลัก**: แสดง Top 5 ผู้เล่น + อันดับของคุณ (ถ้าไม่อยู่ใน Top 5)
- **หน้า Scoreboard**: แสดงผู้เล่นทุกคนพร้อมสถิติละเอียด (Rank, Nickname, Win Rate, Score, Wins, Losses, Draws, Last Updated)
- **เรียงลำดับ**: Win Rate → Score → Wins (สูงสุดมาก่อน)

### ระบบ Nickname
- **ค่าเริ่มต้น**: `playerxxxxx` (xxxxx คือตัวเลขจาก user ID)
- **กฎการตั้ง**: 
  - ความยาว 3-24 ตัวอักษร
  - ใช้ได้เฉพาะตัวอักษร, ตัวเลข, เว้นวรรค, ขีด (-), ขีดล่าง (_)
- **เปลี่ยนได้**: จากเมนู Settings ในเกมส์

### ระดับความยากของบอท
ปรับได้ในไฟล์ `public/app.js` ที่ object `BOT_BEHAVIOUR`:

```javascript
const BOT_BEHAVIOUR = {
  finishChance: 0.95,    // โอกาสที่บอทจะชนะเมื่อมีโอกาส (0-1)
  blockChance: 0.45,     // โอกาสที่บอทจะบล็อกผู้เล่น (0-1)
  centerChance: 0.55,    // โอกาสที่บอทจะเลือกช่องกลาง (0-1)
  cornerChance: 0.6,     // โอกาสที่บอทจะเลือกมุม (0-1)
};
```

ค่า **0.0** = ไม่ทำเลย, **1.0** = ทำทุกครั้ง

## หมายเหตุเพิ่มเติม

- ข้อมูลผู้เล่นเก็บในไฟล์ `data/xo.sqlite` หากต้องการ reset คะแนนทั้งหมด ให้ลบไฟล์นี้
- ระดับความยากของบอทปรับได้ใน `public/app.js` ที่ object `BOT_BEHAVIOUR`
- User ID จะมี prefix ชื่อ provider (`google_xxx`, `facebook_xxx`) เพื่อไม่ให้ซ้ำกัน
- Server ใช้ Express 5 พร้อมฟีเจอร์ `node --watch` สำหรับโหมดพัฒนา
- ชื่อเล่นต้องมี 3-24 ตัวอักษร ใช้ได้เฉพาะตัวอักษร, ตัวเลข, เว้นวรรค, ขีด และขีดล่าง

## เทคโนโลยีที่ใช้

- **Backend**: Node.js + Express 5
- **Authentication**: Passport.js (Google OAuth 2.0, Facebook OAuth 2.0)
- **Database**: SQLite3
- **Frontend**: Vanilla JavaScript, HTML5, CSS3
- **Session**: express-session
- **Styling**: Custom CSS (Dark Neon Theme)

## License

ISC

## ติดต่อ & สนับสนุน

หากพบปัญหาหรือมีข้อเสนอแนะ สามารถเปิด Issue ที่ [GitHub Repository](https://github.com/tanakornW/XO)

---

**สนุกกับการเล่น XO Arena! 🎮✨**

